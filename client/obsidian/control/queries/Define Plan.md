
> [!NOTE] Title
> Contents
```dataviewjs
const helperPath = `${app.vault.adapter.basePath}/_control/scripts/plans/render-create-plan.js`;
const { renderCreatePlan } = require(helperPath);
await renderCreatePlan({ app, dv, container: this.container });
```
