Good, no syntax errors. Let me dig into the actual architecture and look for the failure patterns you've flagged before.Good, that's defined. Let me check the model_base / RedisModel `.load()` method and the `RedisKeyedModel` kind/identity/suffix pattern, plus check for the `extra="forbid"` discipline and any drift.These match the model fields cleanly. Now let me check where these factories are actually called from, and verify the contracts module (action string constants) lines up with what handlers dispatch on.Pulled it apart — solid week of work overall, but the refactor left exactly the kind of debris you've been hunting for in past reviews. Verdict first, then specifics.

**The core data-flow contract is now clean.** You thinned `Outcome` down to a bare receipt (`identity`, `status`, `message`, `created_at`, `extra="forbid"`) and the *live* orchestrator path handles that correctly end to end: `daemon.py` → `handle.py` loads the `Outcome`, re-loads the associated `Task` by identity, and dispatches to `handlers/worker.py` / `handlers/scrivener.py`, both of which read `task.action`/`task.step_key`/`task.data_key` (which exist) and `outcome.status`/`outcome.message` (which exist). `worker/execute.py` writes outcomes with `message=output_key`, which is exactly what the handler reads back out as `result_key`. That loop is tight. And the old `Task.load()` vs `WorkerTask.load()` bug class is gone — checked every `.load()` call site, they're all using the correct concrete subclass now.

**But there's dead code from the Outcome-thinning that's a live landmine, not just clutter:**

`asc/orchestrator/handler.py` (singular) + `asc/orchestrator/handlers/outcome.py` are a second, parallel router still running the *old* fat-Outcome contract — `outcome.package`, `outcome.action`, `outcome.data_key`, `outcome.step_number`, `outcome.result_key`, `outcome.failure_key`. None of those fields exist on `Outcome` anymore. `handler.py:handle_message()` would crash on the first line of `_handle_scrivener`/`_handle_worker` if anything ever called it. Right now nothing does — `daemon.py` correctly imports `handle` from `handle.py` — but `inbox.py` still imports `require_post_key` from the *old* `handler.py`, and `handle.py`/`handler.py` carry duplicate, drifting copies of `require_post_key`/`key_kind`. That duplication is itself how you got here. I'd delete `handler.py` and `handlers/outcome.py` outright and point `inbox.py` at `handle.py`.

**Bigger one: `asc/registries/contracts.py`'s `MODEL_CONTRACTS` is stale across most entries, and it's reachable from the CLI right now.**

- `input:prompt` → `asc.models.uploaded.record.UploadedRecord` / `asc.models.uploaded.prompt.PromptRecord` — `asc.models.uploaded` doesn't exist in this tree at all.
- `input:instruction` → `asc.models.control.instruction.InstructionRecord` — the actual class is `Instruction`.
- `input:plan` → `asc.models.control.plan.PlanRecord` — the actual class is `Plan`. (Same drift shows up in `plan.py`'s own `__all__ = ["PlanRecord"]`, which is also wrong — pyflakes flags it.)
- `export:pending-export` / `export:extracted-result` / `export:export-update` → `asc.models.export.records.*` — the package is `asc.models.exporter`, not `export`. Class names themselves are right, module path isn't.

Only the four `process:*` contracts resolve. `asc registry contract show input:plan` (or `input:prompt`, `input:instruction`, any of the three `export:*` aliases) will fail with `ContractResolutionError` right now. This looks like the models package got reorganized (`uploaded/` dropped in favor of `helpers/upload.py`, `*Record` suffix dropped from `Instruction`/`Plan`, `export` renamed `exporter`) and this registry was never touched. Worth a `resolve_model()` smoke test over every key in `MODEL_CONTRACTS` as a regression guard — this is precisely the "call site not updated when downstream signature changed" pattern, just at the module-path level instead of the function-signature level.

**Medium: Call TTL management is fully disabled, silently.** `enqueuer/call.py`'s `expire_call`/`promote_call_ttl` and the call in `enqueuer/service.py` are all commented out, but `__all__` in `call.py` still exports the dead names (pyflakes catches this too) and the docstrings still describe the probationary-TTL-then-promote design as if it's active. As checked in, every `CallRecord` persists with no TTL at all — not probationary, not promoted. Given you're about to chase `HEXPIRE`/TTL-window behavior on the DO deploy, worth deciding deliberately whether this is paused-on-purpose or just got lost mid-refactor, rather than discovering it's off when Redis memory creeps up under alpha load.

**Trivial cleanup** (pyflakes turned these up, low stakes): duplicate `import time` in `core/timestamp.py` (harmless, just sloppy); unused `sys`/`TextIO` in `cli/export.py`; unused `Any` in `scrivener/schema_dump.py`; unused `Self` in `models/process/step.py`; unused `ulid_obj` in `core/identity.py` — that one's from the age-check block in `validate_ulid` that's commented out, so the parse-and-discard is intentional-ish but worth a `_ = ulid.ULID.from_str(value)` or just dropping the assignment.

One thing that's *not* a problem: no Lua scripts in this snapshot at all — queue claiming goes through `ZPOPMIN`, which is atomic as a single command, so you don't need Lua there. Not a gap, just noting it in case you expected to see the `KEYS`-table pattern and didn't.

Net: the architecture itself is sound and the hot path is clean. The damage is concentrated in exactly two places — the contracts registry and the orchestrator's old router — both classic "renamed it in the models layer, forgot the thing that does dynamic lookup by string." Worth a `grep -rn "Record\b" asc/registries/contracts.py` pass and deleting `handler.py`/`handlers/outcome.py` before the next alpha run.