# LOCAL RUST SERVICE BOUNDARY
# ---------------------------
# Non-secret service configuration lives in this platform tree. External
# environment variables are not configuration inputs.

source_root: /home/jeremy/Work/Loom
service_binary: /home/jeremy/.local/bin/autoscribe-svc

# The following are runtime targets, not configuration sources.
database: /home/jeremy/.local/share/autoscribe/service.sqlite
pandoc_bin: /usr/bin/pandoc
pandoc_filter: /home/jeremy/Work/Loom/platform/pandoc/filters/emit/emit_ndjson.lua
pandoc_parallelism_min: 2
